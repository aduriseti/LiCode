[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "licode-market"
version = "0.1.0"
dependencies = [
    # Dependencies are primarily managed in requirements.txt for this project.
]

[tool.setuptools.packages.find]
where = ["."]
include = ["market*"]
